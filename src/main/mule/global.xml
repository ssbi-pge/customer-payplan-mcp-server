<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:mcp="http://www.mulesoft.org/schema/mule/mcp"
      xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/mcp http://www.mulesoft.org/schema/mule/mcp/current/mule-mcp.xsd">

    <configuration-properties file="config.properties" doc:name="Configuration properties" />

    <http:listener-config name="MCP_HTTP_Listener_Config" doc:name="HTTP Listener Config">
        <http:listener-connection host="0.0.0.0" port="${http.port}" />
    </http:listener-config>

    <mcp:server-config name="MCP_Server_Config" doc:name="MCP Server Config" serverName="customer-payplan-mcp-server" serverVersion="1.0.0">
        <mcp:streamable-http-server-connection listenerConfig="MCP_HTTP_Listener_Config">
            <mcp:session-handling />
        </mcp:streamable-http-server-connection>
    </mcp:server-config>

    <http:request-config name="HTTP_Request_Config" doc:name="HTTP Request Config" basePath="${api.basePath}">
        <http:request-connection host="${api.host}" port="${api.port}" protocol="HTTPS" />
    </http:request-config>

    <http:request-config name="SAPI_Request_Config" doc:name="SAPI Request Config" basePath="${sapi.basePath}">
        <http:request-connection host="${sapi.host}" port="${sapi.port}" protocol="HTTPS" />
    </http:request-config>

    <!-- OAuth Token Request Configuration -->
    <http:request-config name="OAuth_Token_Request_Config" doc:name="OAuth Token Request Config">
        <http:request-connection host="${oauth.token.host}" port="${oauth.token.port}" protocol="HTTPS" />
    </http:request-config>

    <!-- Sub-flow to retrieve OAuth access token -->
    <sub-flow name="get-auth-token-subflow" doc:id="get-auth-token-subflow">
        <http:request method="POST" config-ref="OAuth_Token_Request_Config" path="${oauth.token.path}" doc:name="POST OAuth Token" target="tokenResponse">
            <http:body><![CDATA[#["grant_type=${oauth.grant_type}&username=${oauth.username}&password=${oauth.password}&client_id=${oauth.client_id}&client_secret=${oauth.client_secret}"]]]></http:body>
            <http:headers><![CDATA[#[output application/java --- {
  "Content-Type": "application/x-www-form-urlencoded"
}]]]></http:headers>
        </http:request>
        <set-variable variableName="accessToken" value="#[vars.tokenResponse.access_token]" doc:name="Set Access Token" />
    </sub-flow>

</mule>
