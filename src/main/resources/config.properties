# MCP Server Configuration
# CloudHub 2.0 uses 8081 by default; override via system property if needed
http.port=8081

# MCP Client Configuration
# On CloudHub, use http://0.0.0.0:8081 to self-reference the app's own listener
mcp.server.url=https://0.0.0.0:8081
